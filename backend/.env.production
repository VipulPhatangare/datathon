# Production Environment Variables for VPS Deployment
# Copy this to .env on your VPS server

# MongoDB Configuration
MONGO_URI=mongodb+srv://vipulphatangare:dEi3nGIORwQsLvVs@cluster1.mhcwfmu.mongodb.net/datathon-2026?retryWrites=true&w=majority&appName=Cluster1

# Session Configuration
# ⚠️ Generate a new strong secret for production using:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=vipulphatangare

SESSION_COOKIE_MAXAGE=86400000

# Server Configuration
PORT=4000

NODE_ENV=production

# CORS Configuration
FRONTEND_URL=https://datathon.synthomind.cloud
